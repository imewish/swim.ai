import { Tag } from "./Tag";
import { Output } from "./Output";
export declare class Mark extends Tag {
    readonly _offset: number;
    readonly _line: number;
    readonly _column: number;
    readonly _note: string | null;
    constructor(offset: number, line: number, column: number, note: string | null);
    offset(): number;
    line(): number;
    column(): number;
    note(): string | null;
    min(that: Mark): Mark;
    max(that: Mark): Mark;
    start(): Mark;
    end(): Mark;
    union(that: Tag): Tag;
    shift(mark: Mark): Mark;
    equals(that: unknown): boolean;
    hashCode(): number;
    display(output: Output): void;
    debug(output: Output): void;
    toString(): string;
    private static _hashSeed?;
    private static _zero?;
    static zero(): Mark;
    static at(offset: number, line: number, column: number, note?: string | null): Mark;
}
//# sourceMappingURL=Mark.d.ts.map