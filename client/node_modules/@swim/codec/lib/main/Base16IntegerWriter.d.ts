import { Output } from "./Output";
import { Writer } from "./Writer";
import { Base16 } from "./Base16";
export declare class Base16IntegerWriter extends Writer<unknown, unknown> {
    private readonly _value;
    private readonly _input;
    private readonly _width;
    private readonly _base16;
    private readonly _index;
    private readonly _step;
    constructor(value: unknown, input: number, width: number, base16: Base16, index?: number, step?: number);
    pull(output: Output): Writer<unknown, unknown>;
    static write(output: Output, value: unknown, input: number, width: number, base16: Base16, index?: number, step?: number): Writer<unknown, unknown>;
    static writeLiteral(output: Output, value: unknown, input: number, width: number, base16: Base16): Writer<unknown, unknown>;
}
//# sourceMappingURL=Base16IntegerWriter.d.ts.map