import { Output } from "./Output";
import { Writer } from "./Writer";
export declare class StringWriter<I> extends Writer<I, unknown> {
    private readonly _value;
    private readonly _input;
    private readonly _index;
    constructor(value: unknown, input: string, index?: number);
    feed(input: unknown): Writer<I, unknown>;
    pull(output: Output): Writer<I, unknown>;
    static write<I>(output: Output, value: unknown, input: string, index?: number): Writer<I, unknown>;
}
//# sourceMappingURL=StringWriter.d.ts.map