import { Inlet } from "./Inlet";
export var MapInlet = {
    is: function (object) {
        if (typeof object === "object" && object) {
            var inlet = object;
            return Inlet.is(inlet)
                && typeof inlet.invalidateOutputKey === "function"
                && typeof inlet.reconcileOutputKey === "function";
        }
        return false;
    },
};
//# sourceMappingURL=MapInlet.js.map