import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class AttrWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _key;
    private readonly _value;
    private readonly _part;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, key: V, value: V, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, key: V, value: V): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, key: V, value: V, part?: Writer, step?: number): Writer;
}
//# sourceMappingURL=AttrWriter.d.ts.map