import { Output, Writer } from "@swim/codec";
export declare class MarkupTextWriter extends Writer {
    private readonly _text;
    private readonly _index;
    private readonly _escape;
    private readonly _step;
    constructor(text: string, index?: number, escape?: number, step?: number);
    pull(output: Output): Writer;
    static sizeOf(text: string): number;
    static write(output: Output, text: string, index?: number, escape?: number, step?: number): Writer;
}
//# sourceMappingURL=MarkupTextWriter.d.ts.map