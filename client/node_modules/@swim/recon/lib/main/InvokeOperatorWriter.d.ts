import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class InvokeOperatorWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _func;
    private readonly _args;
    private readonly _part;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, func: V, args: V, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, func: V, args: V): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, func: V, args: V, part?: Writer, step?: number): Writer;
}
//# sourceMappingURL=InvokeOperatorWriter.d.ts.map