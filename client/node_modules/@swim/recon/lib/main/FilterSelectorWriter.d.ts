import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class FilterSelectorWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _predicate;
    private readonly _then;
    private readonly _part;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, predicate: V, then: V, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, predicate: V, then: V): number;
    static sizeOfThen<I, V>(recon: ReconWriter<I, V>, predicate: V, then: V): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, predicate: V, then: V, part?: Writer, step?: number): Writer;
    static writeThen<I, V>(output: Output, recon: ReconWriter<I, V>, predicate: V, then: V): Writer;
}
//# sourceMappingURL=FilterSelectorWriter.d.ts.map