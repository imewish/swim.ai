import { Builder } from "@swim/util";
import { Input, Parser } from "@swim/codec";
import { ReconParser } from "./ReconParser";
export declare class PrefixOperatorParser<I, V> extends Parser<V> {
    private readonly _recon;
    private readonly _builder;
    private readonly _operator;
    private readonly _operandParser;
    private readonly _step;
    constructor(recon: ReconParser<I, V>, builder?: Builder<I, V>, operator?: string, rhsParser?: Parser<V>, step?: number);
    feed(input: Input): Parser<V>;
    static parse<I, V>(input: Input, recon: ReconParser<I, V>, builder?: Builder<I, V>, operator?: string, rhsParser?: Parser<V>, step?: number): Parser<V>;
}
//# sourceMappingURL=PrefixOperatorParser.d.ts.map