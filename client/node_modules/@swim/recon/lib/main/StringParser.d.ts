import { Input, Output, Parser } from "@swim/codec";
import { ReconParser } from "./ReconParser";
export declare class StringParser<I, V> extends Parser<V> {
    private readonly _recon;
    private readonly _output;
    private readonly _quote;
    private readonly _code;
    private readonly _step;
    constructor(recon: ReconParser<I, V>, output?: Output<V>, quote?: number, code?: number, step?: number);
    feed(input: Input): Parser<V>;
    static parse<I, V>(input: Input, recon: ReconParser<I, V>, output?: Output<V>, quote?: number, code?: number, step?: number): Parser<V>;
}
//# sourceMappingURL=StringParser.d.ts.map