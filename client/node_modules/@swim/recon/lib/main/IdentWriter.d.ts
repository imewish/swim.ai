import { Output, Writer } from "@swim/codec";
export declare class IdentWriter extends Writer {
    private readonly _ident;
    private readonly _index;
    constructor(ident: string, index?: number);
    pull(output: Output): Writer;
    static sizeOf(ident: string): number;
    static write(output: Output, ident: string, index?: number): Writer;
}
//# sourceMappingURL=IdentWriter.d.ts.map