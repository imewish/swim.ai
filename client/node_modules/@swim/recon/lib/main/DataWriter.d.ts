import { Output, Writer } from "@swim/codec";
export declare class DataWriter extends Writer {
    private readonly _array;
    private readonly _part;
    private readonly _step;
    constructor(array: Uint8Array, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf(length: number): number;
    static write(output: Output, array: Uint8Array, part?: Writer, step?: number): Writer;
}
//# sourceMappingURL=DataWriter.d.ts.map