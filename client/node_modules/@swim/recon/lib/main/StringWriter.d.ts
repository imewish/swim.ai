import { Output, Writer } from "@swim/codec";
export declare class StringWriter extends Writer {
    private readonly _string;
    private readonly _index;
    private readonly _escape;
    private readonly _step;
    constructor(string: string, index?: number, escape?: number, step?: number);
    pull(output: Output): Writer;
    static sizeOf(string: string): number;
    static write(output: Output, string: string, index?: number, escape?: number, step?: number): Writer;
}
//# sourceMappingURL=StringWriter.d.ts.map