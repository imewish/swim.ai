import { Cursor } from "@swim/util";
import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class PrimaryWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _items;
    private readonly _inParens;
    private readonly _first;
    private readonly _item;
    private readonly _next;
    private readonly _part;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, items: Cursor<I>, inParens?: boolean, first?: boolean, item?: I, next?: I, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, items: Cursor<I>): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, items: Cursor<I>, inParens?: boolean, first?: boolean, item?: I, next?: I, part?: Writer, step?: number): Writer;
}
//# sourceMappingURL=PrimaryWriter.d.ts.map