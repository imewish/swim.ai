import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class LiteralSelectorWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _item;
    private readonly _then;
    private readonly _part;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, item: I, then: V, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, item: I, then: V): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, item: I, then: V, part?: Writer, step?: number): Writer;
}
//# sourceMappingURL=LiteralSelectorWriter.d.ts.map