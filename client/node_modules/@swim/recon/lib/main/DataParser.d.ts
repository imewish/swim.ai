import { Input, Parser } from "@swim/codec";
import { ReconParser } from "./ReconParser";
export declare class DataParser<I, V> extends Parser<V> {
    private readonly _recon;
    private readonly _base64Parser;
    private readonly _step;
    constructor(recon: ReconParser<I, V>, base64Parser?: Parser<V>, step?: number);
    feed(input: Input): Parser<V>;
    static parse<I, V>(input: Input, recon: ReconParser<I, V>, base64Parser?: Parser<V>, step?: number): Parser<V>;
}
//# sourceMappingURL=DataParser.d.ts.map