import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class LambdaFuncWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _bindings;
    private readonly _template;
    private readonly _part;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, bindings: V, template: V, part?: Writer, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, bindings: V, template: V): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, bindings: V, template: V, part?: Writer, step?: number): Writer;
}
//# sourceMappingURL=LambdaFuncWriter.d.ts.map