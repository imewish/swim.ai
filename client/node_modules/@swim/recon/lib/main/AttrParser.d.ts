import { Input, Parser } from "@swim/codec";
import { ReconParser } from "./ReconParser";
export declare class AttrParser<I, V> extends Parser<I> {
    private readonly _recon;
    private readonly _keyParser;
    private readonly _valueParser;
    private readonly _step;
    constructor(recon: ReconParser<I, V>, keyParser?: Parser<V>, valueParser?: Parser<V>, step?: number);
    feed(input: Input): Parser<I>;
    static parse<I, V>(input: Input, recon: ReconParser<I, V>, keyParser?: Parser<V>, valueParser?: Parser<V>, step?: number): Parser<I>;
}
//# sourceMappingURL=AttrParser.d.ts.map