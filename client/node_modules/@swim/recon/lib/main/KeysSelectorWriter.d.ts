import { Output, Writer } from "@swim/codec";
import { ReconWriter } from "./ReconWriter";
export declare class KeysSelectorWriter<I, V> extends Writer {
    private readonly _recon;
    private readonly _then;
    private readonly _step;
    constructor(recon: ReconWriter<I, V>, then: V, step?: number);
    pull(output: Output): Writer;
    static sizeOf<I, V>(recon: ReconWriter<I, V>, then: V): number;
    static write<I, V>(output: Output, recon: ReconWriter<I, V>, then: V, step?: number): Writer;
    static writeThen<I, V>(output: Output, recon: ReconWriter<I, V>, then: V): Writer;
}
//# sourceMappingURL=KeysSelectorWriter.d.ts.map